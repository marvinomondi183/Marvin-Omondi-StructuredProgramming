#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char Str[50];
    char Str[50];

    printf("Enter the string: ");
    scanf("%s",&Str);
    printf("\nThe length of the string '%s' is %zu.",Str, strlen(Str));
    return 0;
}
